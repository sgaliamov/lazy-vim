return {
    'gbprod/yanky.nvim',
    opts = {
        highlight = { timer = 250 }
    }, -- the empty table is required.
--    keys = { -- keys override breaks default bindings.
--	    { "<leader>y", function() require("telescope").extensions.yank_history.yank_history({ }) end, desc = "Open Yank History" },
 --   },
}
